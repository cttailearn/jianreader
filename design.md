# 简阅 —— 技术方案设计文档

> 状态：方案已确认，待开发
> 决策记录：Tauri 2.x / 混合双内核（CodeMirror 6 + Milkdown）/ 编码检测必带 / 自定义顶栏 / 默认隐藏噪音目录 / 安装包+绿色版 / 手动保存为主+可开自动 / 小说模式：仅 txt（epub 远期）、米黄护眼默认背景、自动进入可随时退出

---

## 1. 产品定位

一款**安装到 Windows 电脑端**的轻量文本编辑器，主打「打开快、目录实时同步、Markdown 所见即所得、小说阅读、双主题」。

核心用户场景：

- 开发者/写作者日常浏览代码文件与 Markdown 文档
- 以目录为单位管理一批文本文件，随时查看最新内容
- 阅读与编辑 txt 小说（章节解析、护眼阅读、直接修改、查找替换）
- 需要一个比记事本强、比 VS Code 轻的中间态工具

---

## 2. 技术架构

```
┌─────────────────────────────────────────────────────┐
│                    Tauri 2.x (Rust)                 │
│  ┌───────────────┐  ┌───────────────┐  ┌─────────┐  │
│  │  fs 读写命令   │  │  notify 监听   │  │ 窗口管理 │  │
│  └───────┬───────┘  └───────┬───────┘  └─────────┘  │
└──────────┼──────────────────┼───────────────────────┘
           │ invoke()         │ event (emit/listen)
┌──────────┴──────────────────┴───────────────────────┐
│                WebView2 前端 (Vite + React + TS)     │
│  ┌────────────┐  ┌──────────────┐  ┌─────────────┐  │
│  │ 目录树(虚拟滚动)│  │ 标签页 + 状态栏 │  │ 主题系统    │  │
│  └────────────┘  └──────────────┘  └─────────────┘  │
│  ┌─────────────────────────┐  ┌──────────────────┐  │
│  │ CodeMirror 6 (代码文件)  │  │ Milkdown (MD文件) │  │
│  └─────────────────────────┘  └──────────────────┘  │
│  ┌────────────────────────────────────────────────┐ │
│  │       NovelReader（小说阅读视图，txt 专用）       │ │
│  └────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────┘
```

**分层原则**：文件系统的一切读写与监听都走 Rust（快 + 可靠）；编辑器、渲染、UI 全在前端（Web 生态现成组件）。

### 2.1 壳层选型：Tauri 2.x

| 项 | 结论 |
| --- | --- |
| 安装包 | 3~10 MB（NSIS 打包） |
| 启动 | <1s，WebView2（Win10/11 自带） |
| 目录监听 | Rust `notify` crate（ReadDirectoryChangesW 后端），精确区分 create/modify/remove/rename |
| 权限 | capability 声明式管控 fs 范围，只放开用户主动打开的目录 |

### 2.2 前端框架

- **Vite + React 18 + TypeScript**（Milkdown 官方 React 支持好，生态最大；如偏好 Vue 可在骨架期替换）
- **Zustand** 做状态管理（轻量，无样板）
- **不引重型组件库**，目录树/标签页自研 + 少量 Tailwind CSS 原子类，保证首开速度
- 代码高亮语言包、Milkdown 插件全部**动态 import 按需加载**

### 2.3 双编辑器内核

| 文件类型 | 内核 | 说明 |
| --- | --- | --- |
| 代码（30+ 语言） | **CodeMirror 6** | ~500KB，启动快；@codemirror/lang-* 按需加载；内置搜索、行号、括号匹配、折叠 |
| Markdown | **Milkdown**（ProseMirror 系） | 所见即所得单界面；插件：表格/代码块/图片/任务列表/LaTeX(katex)/Mermaid 图 |
| 其他（txt/log/json/csv 等） | CodeMirror 6 + 对应 language 包 | 超大文件（>5MB）转只读懒加载 |
| 章节型 txt 小说 | **NovelReader 阅读视图**（章内所见即所得编辑 + 全文源码模式兜底） | 章节目录/阅读设置/查找替换/书签，详见 3.8 |

**MD 双模式**：默认所见即所得；状态栏一键切「源码模式」（同一份文档，不丢内容）。

### 2.4 UI 布局

```
┌──────────────────────────────────────────────────────────────────┐
│  📁 项目名          [+新文件] [+新文件夹] [刷新]   [大纲] [🌙/☀]  │  ← 顶栏(紧凑)
├────────────┬──────────────────────────────────┬──────────────────┤
│  📁 目录     │  tab1.ts | tab2.md* | tab3.txt │   MD 大纲(仅md)   │
│  ├─ src     │  ┌────────────────────────────┐ │   # 标题1       │
│  │  ├ a.ts  │  │                            │ │   ## 标题2      │
│  │  └ b.md  │  │   编辑器 (CM6 / Milkdown)   │ │   ### 标题3     │
│  └ readme.md│  │                            │ │   ...           │
│             │  └────────────────────────────┘ │   (可折叠)       │
│  (可折叠)    │  [外部修改提示条占位区]            │   (可折叠)       │
├────────────┴──────────────────────────────────┴──────────────────┤
│ UTF-8 | Markdown | 行 12, 列 34 | ●已修改 | 2.3KB | 缩放 100%     │  ← 状态栏
└──────────────────────────────────────────────────────────────────┘
```

- 三栏：左目录树（可折叠）｜中标签页+编辑器 ｜右大纲（仅 MD 显示，可折叠）
- 顶栏：当前目录名 + 高频操作按钮，不搞系统原生菜单栏（界面更现代、可控）
- 状态栏：编码 / 语言 / 行:列 / 修改标记 / 文件大小 / 缩放；点编码可手动切换
- 窗口：默认 1280x800，记住上次窗口大小/位置/最大化和主题

**小说模式布局**（打开章节型 txt 时切换形态）：

```
┌────────────────────────────────────────────────────────────┐
│ 📖 书名.txt       [阅读设置] [源码] [查找替换]   [🌙/☀]      │  ← 顶栏(上下文按钮)
├───────────────┬────────────────────────────────────────────┤
│  📚 章节目录     │                                        │
│  卷一 第一章     │        「正文阅读区：单栏居中，宽 60~80ch」      │
│  第一章 xxx     │         字号/行距/字间距按阅读设置渲染        │
│  第二章 yyy     │        点击正文 → 该章进入编辑态（直接改）      │
│  第三章 zzz     │                                        │
│  番外           │    [当前章节 x/y | 进度 42% | 上一章/下一章]  │
├───────────────┴────────────────────────────────────────────┤
│ GBK | 小说模式 | 第 3/208 章 | ●已修改 | 4.2MB | 米黄护眼     │  ← 状态栏
└────────────────────────────────────────────────────────────┘
```

- 左侧文件树在小说模式下切换为章节目录（点章即跳）；阅读区背景/字体独立于应用主题
- 阅读模式与普通编辑可一键互切（「源码」按钮回到 CodeMirror 全文编辑）

---

## 3. 核心需求落地方案

### 3.1 需求 1：打开各种文本文件

- 编码识别：Rust 侧读字节 → BOM 判断 → UTF-8 合法性 → 失败则 `chardetng` 检测（GBK/GB2312/Big5 等）→ 返回 `{ text, encoding, hasBom }`
- 保存时**按原编码写回**（GBK 文件存回 GBK），状态栏显示当前编码，可手动切换
- 语言自动检测：扩展名映射表 + 内容启发式

### 3.2 需求 2：快速加载

- 启动链路：Rust 主进程秒起 → WebView 加载本地打包资源（无网络请求）
- 文件打开：首次读盘走 Rust 命令，小文件 <1ms；大文件流式读取 + 懒渲染
- 标签页内容缓存 + 目录树虚拟滚动（万级文件不卡）

### 3.3 需求 3：目录实时同步（核心难点）

```
外部修改 → notify 事件 → Tauri emit → 前端目录树增量更新
外部修改已打开的文件：
  ├─ 本地未改 → 自动重载内容
  └─ 本地已改 → 顶部提示条：「文件已被外部修改」[重新加载] [保留本地]
软件保存 → invoke(fs_write) → 写盘 → notify 回环事件 → 前端按 mtime+内容hash 忽略自身写入
目录变更（增/删/改名）→ 目录树增量刷新，已打开的标签页若文件被删则标记「已删除」
```

关键点：**自身写入回环抑制**（保存后短暂忽略同一路径事件，或比对内容 hash），避免保存后闪一下重载。

### 3.4 需求 4：MD 所见即所得

- 编辑即渲染，单界面；光标处实时高亮
- 支持：标题/粗斜体/链接/图片/表格/代码块(带高亮)/任务列表/引用/分割线/LaTeX 公式/Mermaid 流程图
- 图片支持：本地相对路径图片（相对于文件所在目录）直接渲染
- 粘贴 Markdown 文本自动转富文本；复制富文本转 Markdown 源码

**标题目录（大纲）+ 跳转**：

- 数据源：Milkdown `plugin-listener` 监听 doc 变化 → 遍历 ProseMirror doc 提取全部标题节点（level + 文本 + pos）→ 生成大纲树（H1~H6 按层级缩进，可折叠子级）
- 位置：右侧可切换侧栏（工具栏按钮 + 快捷键），无标题时显示占位提示
- 点击跳转：`view.dispatch` 滚动定位到标题 pos 并把光标落点该标题行
- 滚动跟随（scrollspy）：监听编辑器滚动，高亮当前所在章节对应大纲项；编辑时大纲**实时同步**更新
- 仅 MD 文件显示；切换标签页时大纲跟随切换

### 3.5 需求 5：浅色/暗色主题

- CSS 变量体系（语义 token：bg / surface / border / text / accent / code-bg…）
- 三处联动：应用 UI、CodeMirror 主题、Milkdown 主题
- 切换：状态栏按钮 + 快捷键（Ctrl+Shift+L / T），持久化到 localStorage
- 跟随系统可做（P2）：`prefers-color-scheme` 默认值

### 3.6 文档状态机（文件生命周期）

```
closed ─open→ loading ─→ ready ─edit→ dirty ─save→ ready
               │             ▲                    │
               ▼             │                    ▼
             error(打不开)    └──── saving(写盘失败→提示重试)

ready/dirty ─外部修改事件→ external-changed
   ├─ 本地未修改 → 自动重新加载 → ready
   └─ 本地已修改 → 顶部提示条 [重新加载] [保留本地]

ready/dirty ─磁盘上被删除→ deleted（标签灰化 + 提示，保存时重建文件）
```

关键点：状态全部在 Zustand tabs store 里驱动，UI 只做状态渲染；`dirty` 时关闭标签必须弹确认（保存/不保存/取消）。

### 3.7 交互入口

| 动作 | 方式 |
| --- | --- |
| 打开目录 | Ctrl+O 或顶栏按钮或拖拽文件夹到窗口 |
| 打开文件 | 双击目录树 / 拖拽文件到窗口 / Ctrl+P 快速打开（P1） |
| 目录树右键 | 打开 / 新建文件 / 新建文件夹 / 重命名 / 删除(回收站) / 在资源管理器中显示 |
| 编辑器右键 | 撤销重做 / 剪切复制粘贴 / 全选 / 查找 |
| 标签操作 | 双击空白新建 / 滚轮切换 / 中键关闭 / 拖拽排序(P1) |

### 3.8 需求 6：小说模式（txt 阅读 + 编辑）

**触发与判定**：打开 .txt 文件时流式扫描章节标题特征：

- 命中 ≥3 个标准章节标题 → **自动进入小说模式，零打断**；状态栏显示「小说模式」可随时切回普通编辑
- 未命中 → 普通编辑；顶栏「阅读模式」按钮可手动切换
- 格式边界：仅 txt（epub 需拆 zip+HTML，列入 P2 远期）

**章节解析器**（Rust 侧流式扫描 + 前端工具库）：

| 模式 | 示例 |
| --- | --- |
| 中文章节 | 第一章 / 第12章 / 第100节 / 第X回 |
| 卷部集 | 第一卷 / 第二部 / 上卷 / 番外 |
| 特殊章 | 序章 / 楔子 / 引子 / 前言 / 尾声 / 后记 / 终章 / 正文 |
| 数字编号 | 行首纯数字标题（1 / 12 / 100） |
| 英文 | Chapter 1 / Part II / Episode 5 |

- 判定评分：标题命中数 + 行首位置 + 行短（<30字）综合判断，降低散文误判
- 产出章节表 `{ title, startOffset, endOffset }`（仅元数据），正文按章懒加载
- 误判修正：右键「设为章节标题 / 取消章节」（P1）

**阅读设置**（每本书独立记忆，localStorage 按绝对路径存）：

- 字号 14~36px / 行距 1.5~2.5 / 字间距 / 段间距 / 正文宽度 60~80ch
- 字体：中文优先（微软雅黑、思源黑体等系统字体）
- 阅读背景与应用主题解耦：浅色 / 米黄护眼 / 浅灰 / 夜间纯黑，**默认米黄护眼**（#F5EFE0 类），顶栏弹出面板即时生效

**直接编辑**：

- 阅读视图点击正文 → 当前章进入编辑态（所见即所得纯文本，光标/选区保留）
- 保存：按原编码写回整文件（按章节 offset 定位），保存后重解析章节表（章节结构可能已变）
- 「源码」按钮切 CodeMirror 全文编辑，与普通 txt 同一套内核

**查找替换**：

- Ctrl+F：当前章内即时搜索（高亮 + 计数），可展开到全书（虚拟化结果列表，点击跳章）
- 替换：当前章 / 全书两种范围，逐条确认或全部替换；替换后对应章标 dirty
- 源码模式下用 CodeMirror 自带查找替换

**书签续读**：

- 每本书记录：最后阅读章节 + 章内滚动位置 + 阅读设置
- 再次打开 → 提示「继续阅读」直达上次位置；自定义书签可存多个（P1）

**性能**：50MB txt（约 5000 万字）打开 < 1s（Rust 流式扫描章节表，不整读内存）；正文按章懒加载，内存只保留当前章 ± 邻章

---

## 4. 功能清单

### P0（首版必须）

- [x] 目录树：打开目录、折叠展开、文件图标、双击打开、右键菜单（打开/重命名/删除/新建文件/新建文件夹）
- [x] 目录树默认忽略噪音目录（.git/node_modules/dist 等，不影响性能与观感）
- [x] 多标签页：切换、关闭、未保存圆点标记、拖拽排序（P1）
- [x] 编码检测 + 原编码写回（UTF-8/UTF-8 BOM/GBK/GB2312/ASCII 等）
- [x] 代码高亮 30+ 语言、行号、自动换行切换、Ctrl+F 搜索
- [x] MD 所见即所得（含表格/代码块/图片/任务列表/公式/Mermaid）
- [x] MD 标题目录（大纲）：实时生成、点击跳转、滚动跟随高亮
- [x] 目录实时双向同步（外部→软件、软件→磁盘）
- [x] 小说模式：章节智能解析、阅读设置（字号/行距/背景）、章内直接编辑、查找替换、书签续读
- [x] 浅色/暗色主题三联动
- [x] 状态栏：当前编码 / 语言 / 行:列 / 修改标记 / 文件大小
- [x] 快捷键：Ctrl+S 保存、Ctrl+O 打开目录、Ctrl+W 关标签、Ctrl+Tab 切换
- [x] 换行符保留：读取时记录 CRLF/LF，保存时按原样写回（避免 git diff 噪音）
- [x] 只读文件处理：磁盘只读属性时禁止编辑，状态栏显示🔒
- [x] 未保存关闭确认：dirty 标签关闭时弹「保存/不保存/取消」

### P1（体验增强，后续迭代）

- [ ] 大文件保护（>5MB 只读懒加载）
- [ ] 会话恢复（重启回到上次目录与标签）
- [ ] 目录内全局搜索（结果列表 + 点击跳转）
- [ ] 最近打开目录列表
- [ ] 标签拖拽排序
- [ ] MD 导出 HTML
- [ ] Ctrl+P 快速打开（输入文件名模糊匹配当前目录，命令面板式）
- [ ] 隐藏文件/忽略目录的显示开关（设置里切换，默认隐藏）
- [ ] 自动保存开关（默认关闭，可开启，dirty 后 2s 自动写盘）

### P2（远期可选）

- [ ] 小说 epub 格式支持（zip+XHTML 解析渲染）
- [ ] 跟随系统主题、自定义 accent 色
- [ ] 多工作区（一次开多个目录）
- [ ] 文件对比 / 迷你地图 / 终端
- [ ] 设置面板（字体、字号、Tab 宽度、行号开关）

---

## 5. 项目目录结构

```
文本查看编辑器/
├── design.md                 # 本文档
├── index.html
├── package.json
├── vite.config.ts
├── src/                      # 前端
│   ├── main.tsx
│   ├── App.tsx               # 布局：目录树 | 标签页区 | 状态栏
│   ├── components/
│   │   ├── TopBar.tsx        # 顶栏（目录名/新建/刷新/大纲开关/主题）
│   │   ├── FileTree.tsx      # 目录树（虚拟滚动 + 增量更新）
│   │   ├── ChapterPanel.tsx  # 小说章节目录侧栏（解析结果/点击跳章）
│   │   ├── TabBar.tsx        # 多标签
│   │   ├── StatusBar.tsx
│   │   ├── ExternalChangeBar.tsx  # 外部修改提示条
│   │   └── TocPanel.tsx     # MD 大纲侧栏（生成/跳转/滚动跟随）
│   ├── editors/
│   │   ├── CodeEditor.tsx    # CodeMirror 6 封装（语言按需加载）
│   │   ├── MarkdownEditor.tsx# Milkdown 封装（插件装配）
│   │   └── index.tsx         # 按扩展名分发到对应内核
│   ├── readers/
│   │   └── NovelReader.tsx   # 小说阅读视图（章节渲染/编辑态/设置面板/查找替换）
│   ├── stores/
│   │   ├── tabs.ts           # 标签/文档状态（dirty、content、encoding…）
│   │   ├── tree.ts           # 目录树状态（增量 patch）
│   │   ├── reader.ts         # 阅读设置/书签/阅读进度（按路径记忆）
│   │   └── theme.ts
│   ├── styles/
│   │   ├── tokens.css        # 语义色 token（双主题）
│   │   └── global.css
│   └── utils/
│       ├── language.ts       # 扩展名→语言映射
│       ├── novel.ts          # 章节解析器（正则库 + 命中评分）
│       └── tree.ts           # 树 diff/patch 算法
└── src-tauri/                # Rust 壳
    ├── Cargo.toml
    ├── tauri.conf.json
    ├── capabilities/default.json
    └── src/
        ├── main.rs           # 窗口、命令注册
        ├── fs.rs             # 读文件(编码检测)/写文件(原编码回写)
        └── watcher.rs        # notify 目录监听 + 事件发射
```

---

## 6. 依赖清单（关键）

**Rust**：`tauri`(2.x)、`tauri-plugin-fs`、`tauri-plugin-dialog`（选目录）、`tauri-plugin-opener`（资源管理器中显示）、`notify`、`chardetng`、`encoding_rs`（GBK 转换）、`serde`

**前端**：

- `@tauri-apps/api`（invoke/event/convertFileSrc）、`@tauri-apps/plugin-dialog`、`@tauri-apps/plugin-opener`
- `@codemirror/*`：state/view/language/commands/search + `@codemirror/lang-*` 语言包 + `@codemirror/theme-one-dark`（暗色参考）
- `@milkdown/*`：core/preset-commonmark/preset-gfm/plugin-table/plugin-math/plugin-emoji + `remark-math` + `katex` + `mermaid` + `prosemirror*`
- `zustand`、`tailwindcss`（或纯 CSS 变量）、`monaco` 不需要

---

## 7. 性能实现方案（如何做到快）

性能问题按关键路径拆解：启动 → 打开文件 → 编辑器交互 → 目录树/监听 → 内存。每个环节有明确的优化方向。

### 7.1 启动链路（冷启动 < 1.5s）

- Rust 主进程 ~10ms 起窗，WebView 加载**本地打包资源，零网络请求**
- 首屏 JS 预算 < 300KB（gzip）：Vite 手动分包，**编辑器内核全部动态 import**——启动时只加载壳 UI（布局/顶栏/状态栏），CM6 / Milkdown / NovelReader 都是点开文件那一瞬才加载
- 关键 CSS 内联进 index.html（无 FOUC 闪烁）；字体用**系统字体栈**（微软雅黑等），零字体下载
- 目录树数据异步拉取，不阻塞首帧渲染

### 7.2 文件打开关键路径（分档策略）

| 文件大小 | 策略 |
| --- | --- |
| 0~1MB | 一次性 read + 编码检测 + 全量加载，目标 <100ms |
| 1~5MB | 全量读入，CM6 文本切片，初始只渲染视口内行 |
| >5MB | 只读 + 按行分块拉取 + 虚拟化（懒加载） |
| 小说 txt | Rust 流式扫描章节表 → 按章懒加载，50MB 目标 <1s |

**IPC 大文本传输（本项目最关键的性能坑）**：

- 默认 invoke 返回字符串要过 JSON 序列化，几十 MB 文本走这条路会卡顿
- 方向 1：Tauri 2 **raw IPC**（`tauri::ipc::Response` 直接返回原始 bytes，跳过 JSON）
- 方向 2：自定义 `stream://` 协议，前端用 fetch **流式读取**，天然支持分块/断点续读，大文件不整进内存
- 结论：小文件走常规 invoke；大文件/小说章节走 raw IPC 或 stream 协议（M3 前定稿，先做 raw IPC）

**预处理下沉 Rust**：章节解析、行索引、编码检测全部在 Rust 侧完成，前端只收结构（章节表/文本块），前端零重算。编码检测用 SIMD 快速路径（UTF-8 合法性校验 O(n) 极快），非 UTF-8 才走 chardetng，结果缓存。

### 7.3 编辑器渲染性能

- **CM6**：lezer 增量解析（只解析改动区段）+ 自带视口虚拟化；限制历史深度（~1000 步）、关闭拼写检查；单行 >10k 字符启用软换行保护
- **Milkdown**：文档 >1MB 自动提示切源码模式（已有）；长文渲染加 `content-visibility: auto` 跳过屏外布局
- **小说阅读**：每章 DOM 仅 3~10KB 无压力；切章用内容替换而非重建编辑器实例；滚动位置用 scrollTop 记忆恢复

### 7.4 目录树与监听

- 目录树虚拟滚动（@tanstack/react-virtual）：万级文件只渲染视口 30~50 行
- **懒加载子树**：展开目录时才 invoke 读取该层（不做全量递归），根目录也是分层拉取
- notify 事件：Rust 侧 300ms debounce 合并 → 一次 emit 批量 patch；前端用 Map 结构增量更新，不做全量重建
- 忽略噪音目录（.git/node_modules/dist）从源头减少扫描量

### 7.5 内存策略

- 标签页 LRU：>12 个标签的旧文档释放内容（保留路径/编码/滚动位置元数据，切回重读）
- 大文件不整存：小说存章节表 + 当前章±邻章；大代码文件分块
- 图片不经 base64，走 asset 协议 URL

### 7.6 测量与回归

- Rust 侧埋点：窗口创建耗时、WebView 就绪时间
- 前端 performance.mark 埋点：打开文件各阶段、目录树加载、事件延迟
- 每个里程碑交付前对照第 8 章验收指标表逐项过

---

## 8. 性能验收指标

| 指标 | 目标 |
| --- | --- |
| 冷启动到可交互 | < 1.5s |
| 打开 < 1MB 文件 | < 100ms |
| 打开 10MB 代码文件（只读懒加载） | < 1s |
| 打开 50MB 小说 txt（章节索引） | < 1s，按章懒加载，内存只留当前章±邻章 |
| 目录树 1 万文件 | 加载 < 500ms，滚动 60fps |
| 外部文件变更 → UI 更新 | < 300ms |
| 同时 20 个标签页内存占用 | < 300MB |
| 安装包体积 | < 15MB |

## 9. 里程碑

| 阶段 | 内容 | 验收 |
| --- | --- | --- |
| **M1 骨架** ✅ 已完成 | Tauri + Vite 脚手架、窗口、双主题 token、状态栏占位 | 空窗口启动 <1s，可切换主题 |
| **M2 文件浏览** ✅ 已完成 | 目录树 + 打开文件 + 编码检测 + 代码高亮 + 多标签 + 保存 | 能浏览任意目录、正确显示 GBK/UTF-8、编辑保存 |
| **M3 实时同步** ✅ 已完成 | notify 监听 + 事件推送 + 目录树增量 + 外部修改提示 + 回环抑制 | 外部改文件软件实时更新；软件保存外部立刻可见 |
| **M4 MD 所见即所得** | Milkdown 装配（表格/代码/图片/公式/Mermaid）+ 大纲侧栏跳转 + 主题联动 + 源码模式 | MD 文档单界面编辑即预览，大纲点击跳转、跟随滚动 |
| **M5 小说模式** | 章节解析 + 阅读视图 + 阅读设置 + 章内直接编辑 + 查找替换 + 书签续读 | 50MB 小说 1s 内打开，目录跳转、设置生效、编辑保存回写 |
| **M6 打磨打包** | 大文件保护、会话恢复、快捷键完善、NSIS 安装包 + 绿色版 zip | 出 installer.exe 和绿色版，全新机器可安装运行，U 盘可拷走即用 |

---

## 10. 风险与对策

| 风险 | 对策 |
| --- | --- |
| WebView2 老版本兼容 | 最低要求 Win10 1809+，tauri 自动检测缺失提示 |
| notify 事件风暴（大目录/编译产物） | Rust 侧节流（300ms 合并）、忽略常见噪音目录（node_modules/.git/dist） |
| 保存回环触发重载闪烁 | 保存后记录 path+mtime 白名单，300ms 内忽略同名事件 |
| Milkdown 大文档卡顿 | 文档 >1MB 提示转源码模式 |
| GBK 无法 100% 识别 | 识别失败给出候选编码手动切换 UI |
| 本地图片被 WebView 安全策略拦截（file:// 禁用） | 注册自定义 asset 协议：`/asset/*` → 映射为「当前文件所在目录」的真实路径，MD 中相对路径图片可直接显示 |
| 大目录监听性能（万级文件） | 只监听用户打开的根目录 + 必要子目录，忽略 .git/node_modules/dist 等噪音目录 |
| 章节解析误判（散文/杂记被当小说） | 命中评分阈值 + 未命中回退普通编辑；右键手动「设为章节/取消章节」(P1) |
| 50MB 超大 txt 整读内存爆炸 | Rust 流式扫描章节表（不进内存），正文按章懒加载，章内再超限才转只读 |
| IPC 大字符串 JSON 序列化卡顿 | 大文件走 raw IPC（原始 bytes）或自定义 stream 协议流式读取，小文件才走常规 invoke |

# 更新日志 Changelog

## 0.3.2 - 2026-09-02 —— 全量审查修复（R-01~R-28）

> 依据 `docs/审查报告-简阅-2026-09-02.md` 实施的全面修复：数据安全 / 安全纵深 / 正确性 / 性能 / 工程基建。

### 数据安全（P0）
- 小说模式改为「分章草稿」：编辑内容按章保存草稿，切章/退出/重载不再丢弃；保存一次性写回全部已修改章节（倒序保证字节偏移有效）；保存期间继续输入会保留并提示再次保存
- 关闭窗口前拦截：存在未保存修改时弹「保存并退出/不保存退出/取消」，杜绝静默丢失
- 保存冲突检测：普通标签与小说均在保存起点做快照，写盘期间的新输入不再被静默丢弃

### 安全
- 启用严格 CSP（生产 `default-src 'self'`、无内联脚本；开发 `devCsp` 兼容 Vite HMR）
- asset 协议作用域从「全部放开」收窄为按「用户打开的目录」动态放行
- 全部文件命令增加路径作用域校验；删除改走**回收站**（Windows `SHFileOperationW`），重命名拒绝分隔符/`..`
- 更新器命令强制校验在应用更新目录内、仅 `.exe`、大小上限与最小体积
- 多窗口最小能力集拆分（`capabilities/workspace.json`）；Markdown 远端图片加 `no-referrer`
- **更新数字签名验签（ECDSA P-256，GitHub API 方案）**：更新来源改为 `releases/latest` API（**不再需要单独维护/上传 latest.json**）；发布时 `scripts/sign-release.mjs` 把 `sig: <base64>` 写入 Release 备注，客户端 WebCrypto 对 `version+sha256` 验签后下载（fail-closed，公钥固化于 `src/utils/updateVerify.ts`）；新增 `verify-manifest.mjs` 发布前自检

### 正确性
- UTF-16 无 BOM 文件保存不再被强制加 BOM；UTF-16 章节扫描按大小端正确解码/剥 BOM/算偏移（新增 LE/BE 测试）
- `read_chapter` 区间钳制 + 单次 64MB 上限；`write_chapter` 校验 `start<end`（防止损坏文件）
- watcher 改为 `(窗口,目录)` 键、事件风暴下依然可停止、互斥体防中毒；外部修改分叉状态持久标记、保存前确认覆盖
- 对话框改请求队列（并发确认不再卡死）；contenteditable 编辑改逐行 `textContent` 往返（不再折叠空格/缩进）
- 文件保存/章节写回改**原子写**（临时文件 + 重命名）

### 性能
- 大文件硬上限（>96MB 拒绝整读）+ >8MB 标记提示
- **大文本传输改 raw IPC**：章节读取返回 `tauri::ipc::Response` 原始字节，前端 `TextDecoder` 解码，几十 MB 文本不再 JSON 序列化/解析
- **标签内存上限（LRU 式逐出）**：≥4MB 的非激活标签自动清空内容、仅留元数据，切回时重读——「20 标签 <300MB」目标可达成
- 章节扫描改**流式**（BufReader 按行，50MB 不进整块内存），编码检测用「头+尾 256KB」有界采样
- .txt 打开只扫描一次并复用结果（编码/EOF）；目录树刷新按父目录聚合；事件源过滤噪音目录；万级目录展开不再逐文件取大小
- CodeMirror 内核改动态导入（不再静态打进首屏入口）
- **小说阅读段落渐进式渲染**：初始只渲染一屏+缓冲，滚动到底自动扩展，查找跳转先把目标段落纳入窗口——超大单章（整本「全文」）不再一次性渲染几万行 `<p>`，深位置书签恢复会逐次扩窗并锁存

### 工程
- 新增 GitHub Actions CI（tsc + eslint + 前端构建 + cargo test）；接入 ESLint（`npm run lint`）
- 发布脚本新增三处版本一致性校验；localStorage 键统一 `jianyue-*`（兼容迁移）；`design.md` 结构/状态更新
- 无障碍：对话框/标签栏/窗口按钮/编辑区补齐语义与 aria

### 遗留（需真机/外部设施）
- 更新包 Authenticode 代码签名与失败回滚（需代码签名证书）
- 小说 DOM 级虚拟化已实现（渐进式）；仅「几十万段落 + 拉到极底」的极值场景仍需真机验证

---

## 0.3.1 - 2026-09-01

### 修复
- 修复「软件更新」检查/下载在部分网络环境报“网络错误 / Failed to fetch”的问题（下载改走 Rust curl，绕过 GitHub CDN 的 CORS 限制）
- 修复设置为文件关联“默认程序”后，双击文件打开的是上次目录而非该文件的问题（读取系统启动参数优先打开对应文件）
- 修复暗色模式下 Markdown 代码块在切换主题后“黑字黑底”不可见的问题（编辑器随主题重新挂载，代码块高亮主题跟随）

### 新功能
- 设置面板新增：正文字号调节、正文/小说字体选择（微软雅黑 / 宋体 / 楷体 / 仿宋 / 黑体 / 等线 / 幼圆）
- 新增开关：自动保存、显示隐藏文件、大文件语法高亮
- 状态栏显示字符/行统计与版本号
- 浅色主题正文背景由护眼米黄调整为浅灰白（近白非纯白，更清爽不刺眼）
- 目录左侧栏空状态不再显示提示文字

## 0.3.0 - 2026-08-31

### 新功能
- 新增「软件更新」：启动自动检查 + 设置面板手动检查 GitHub Releases 新版本（可在设置中关闭自动检查）
- 发现新版本后右下角横幅提示，一键下载（实时进度）→ SHA-256 双重校验 → 静默安装，重启后生效
- 更新源：GitHub Releases 的 `latest.json`（零新增后端依赖：前端 WebView 下载 + Rust 纯 std 落盘/校验/安装）
- 新增发布脚本 `scripts/release.ps1`：构建 → SHA-256 → 生成 `latest.json` → GitHub Release

### 其他
- 新增 CDP 调试脚本：`scripts/cdp_shot.js`（截图）、`scripts/cdp_monitor.js`（监听 + 轮询），与已有 `cdp_eval/cdp_console` 配套
- README：补充「软件更新」功能说明与「发布新版本」流程
